# Local docs (`apps/docs-dev`)

**Local dev only** — feature planning and WIP docs. Not built or deployed.

WIP MDX lives under `content/tmp/` (gitignored). Authoring conventions live in [`apps/docs`](../docs) and the **use-docs** skill.

```bash
pnpm --dir apps/docs-dev run dev   # http://localhost:5175
```
