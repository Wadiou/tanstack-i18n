---
title: Local dev docs (stub)
description: WIP MDX lives in content/tmp/ — gitignored, not tracked.
---

This file is **not** served by docs-dev. Fumadocs reads [`content/tmp/`](./tmp/) only.

```bash
pnpm --dir apps/docs-dev run dev   # http://localhost:5175
```

Public deployable docs: [`apps/docs`](../docs) (`pnpm docs:dev`).
